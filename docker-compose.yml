version: '3.8'

services:
  bot-ally:
    build: .
    container_name: bot-ally
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/Sao_Paulo
